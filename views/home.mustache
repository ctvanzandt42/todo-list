<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>To-do List</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="header">
        <h2>To-Do List</h2>
    </div>
    <div class="add-form">
        <h4>Add a task!</h4>
        <form class="add-tasks" action="/todo" method="post">
            <input type="text" name="todo" placeholder="Add a task here!" required>
            <button type="submit">Add task!</button>
        </form>
    </div>
    <div class="not-completed-tasks">
        <h4>Tasks to Complete!</h4>
        {{#todos}}
        <form class="todos-to-complete" action="complete/{{todo}}" method="post">
            <button class="complete" type="submit" name="todo" value="{{todo}}">Finish!</button>
            <li>{{todo}}</li>
        </form>
        {{/todos}}
    </div>
    <div class="completed-tasks">
        <h3>Completed Tasks</h3>
        {{#markoff}}
        <p>
            {{todo}}
        </p>
        {{/markoff}}
    </div>


</body>

</html>